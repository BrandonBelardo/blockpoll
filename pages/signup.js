import styled from "styled-components";

const NewHeader = styled.h1`
    padding-top: 5rem;
`

export default function Signup() {
    return(
        <NewHeader>Signup page; to be implemented with backend using crypto wallet</NewHeader>
    );
}